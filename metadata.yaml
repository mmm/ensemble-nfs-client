ensemble: formula
name: nfs-client
revision: 8
summary: NFS client test formula
description: |
  Provides an NFS client to test the primary NFS formula.
  This is intended to be an example formula only.  It shows
  which hooks formulas need to implement to consume the
  mount interface for and nfs filestore service.
requires:
  filestore:
    interface: mount
    optional: true
